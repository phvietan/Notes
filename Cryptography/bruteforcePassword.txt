encrypt-decrypt file:
  $1$: it uses MD5.
  $5$: it uses SHA-256.
  $6$: it uses SHA-512.
  $2a$: it uses blowfish, not supported everywhere.
  Otherwise it uses DES.

Use John to decrypt weak password for user. It uses brute force attack.
